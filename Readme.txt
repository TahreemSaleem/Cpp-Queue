Task1:

(a) Implement the Enqueue() method. A queue class will typically contain a Node reference for the back of the queue that is set to null at construction time to indicate an empty queue.
(b) Implement the Dequeue() method. The method should remove the item at the front of the queue. 
(c) Implement the Front() method. The method should return the value at the front of the queue. 
(d) Implement the Back() method. The method should return the value at the back of the queue.
Execute the completed code by en-queuing and de-queuing 100, 200, 300, 400, 500. 

Task2:

Palindrome checker